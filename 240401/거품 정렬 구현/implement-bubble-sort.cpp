#include <bits/stdc++.h>
using namespace std;

int n;

int main(){
    cin>>n;

function bubble_sort(qrr[])
set len=n.size
set sorted=true

do{
    sorted=true
    for(i=0;i<len-1)
    if(a[i]>a[i+1])
    set tmp=a[i];
    a[i]=a[i+1];
    a[i+1]=tmp
    sorted=false
}
while(sorted==false)

return arr;

}